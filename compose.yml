services:
  ticket-store-frontend:
    image: failcase/ticket-store-frontend:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped

